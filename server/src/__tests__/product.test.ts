describe("product", () => {
  describe("get product route", () => {
    it("should return 404 when product not exists", async () => {
      const productId = "123";

      expect(true).toBe(true);
    });
  });
});
